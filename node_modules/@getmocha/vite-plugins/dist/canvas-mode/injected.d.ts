declare global {
    interface Window {
        __mochaCanvasModeHandlersRegistered?: boolean;
    }
    interface ImportMeta {
        hot?: {
            send: (type: string, data: any) => void;
            on: (event: string, callback: (data: any) => void) => void;
        };
    }
}
export {};
